import { Offer } from '../../models/offer';

// OFFERS
export const changeOfferStatus = (offer: Offer, accepted: boolean) => {};

export const rejectAllOtherOffers = (carId: string, offerId: string) => {};